version: 0.0
os: linux
files:
  - source: /
    destination: /home/ec2-user/sample.daytrader7

hooks:
  AfterInstall:
    - location: /bin/bash
      timeout: 600
      runas: ec2-user
      args:
        - deploy.sh
